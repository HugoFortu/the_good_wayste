<%= render 'shared/banner-search', action: "Vous n'êtes plus à #{} ?" %>

<div class="container">

  <h2 class="font-black mt-10 mb-5 ">Mes Favoris</h2>

  <% if @products.present? %>
      <p class="mb-10">Retrouvez facilement les consignes de tri, même en vous déplaçant</p>
      <% @products.each do |product| %>
        <%= render 'shared/card-fav', product: product %>
      <% end %>
  <% else %>
    <p class="text-justify"><%= "Vous n’avez aucun favoris pour le moment. Ajouter les en cliquant sur “*” sur chaque produit. Ainsi, vous retrouverz facilement les consignes de tri, même en vous déplaçant!" =%></p>
  <% end %>

</div>
